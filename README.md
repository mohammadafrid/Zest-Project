Project Name is Employee

How To run the project 
-> Oen the project in intellij and add JDK17 
-> Find the class Employee Application under  SRC > main > java > find Employee Application with annotation @SpringBootApplication class run it because that is the main class without running it the project doesn't work .

Dependencies i have used 
Lombook
Spring Data JPA
MySql Driver 
Spring Security
DevTool



# Zest-Project
A Spring Boot application that connects to a relational database (MySql/Oracle) using JPA/Hibernate. Implement a CRUD application for managing employee records, including features like pagination and sorting. Secure the application using JWT for authentication and authorization.

